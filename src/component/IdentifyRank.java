package component;

import model.CardDeck;

public class IdentifyRank {


    public void execute(CardDeck firstHandSublist, CardDeck secondHandSublist, Integer firstHandCounter, Integer secondHandCounter) {
        //calculate rank based on set of cards and set the object

        //verify which rank is higher and add up the winner counter
        //if both ranks are equal, should compare the highest card instead

    }
}
